{
  "name": "@gc/online",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Lobby, signalling, session coordination, diagnostics.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsgo --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@gc/core": "workspace:*",
    "@gc/transport": "workspace:*",
    "@gc/wasm": "workspace:*"
  }
}
