[package]
name = "gc-sim"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
gc-core.workspace = true
gc-data.workspace = true
indexmap.workspace = true

# Dev-only: the counting global allocator crates/gc-test-alloc/tests/env_budget.rs
# installs. Never a normal dependency, so it never enters a shipping build,
# and gc-sim's own [lints] below still forbids unsafe code in this crate's
# own source.
[dev-dependencies]
gc-test-alloc.workspace = true

[lints]
workspace = true
