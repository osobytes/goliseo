{
  "name": "@gc/input",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Input capture and binding (browser side of the boundary).",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsgo --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@gc/core": "workspace:*",
    "@gc/ui": "workspace:*"
  }
}
